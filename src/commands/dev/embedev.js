const { MessageEmbed } = require("discord.js");

module.exports.run = async (client, message, args, prefix) => {
  
  var target = false,
      title = false,
      des = false,
      img = false,
      thumbnail = false;
 
}

exports.help = {
  name: "embedev",
  description: "buat liat anu wkwkwkkw",
  usage: "embedev <type> <#channel> <content>"
};

exports.conf = {
  aliases: ["embev"],
  cooldown: 1
};
